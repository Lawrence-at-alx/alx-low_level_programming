#!/bin/bash
#include <stdio.h>
/*
 * main- serve as an entry point to the program
 *Return: By default returns zero if no errors otherwise returns a  non-zero value.
 */
int main(void)
{
	put("\"Programming is like building a multilingal puzzle");
	return (0);
}
